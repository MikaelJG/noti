// see port!

// requesting data on the server
// client is asking for something to display
// client doesn't want to update or change something on the server!

let url = '/theURLtheclientgoesto/lol'
app.get(url);

create many pages by creating different urls

app.get('/', (request, response) => {
   // request - user's incoming data
   // response - you outgoing data (server sending)
});

app.get('/', (request, response) => {
    readFile('./home.html', /utf8', (err, html) => {
        if (err) {
            response.status(500).send('sorry, out of order')
        }
        reponse.send(html)
    }
}
