// every change state, render is called


class TodoList extends React.Component {
    editItem (name) {
        cont todos = [...this.state.todos, name]
        todos[index] = name
        this.setState({todos: todos})
    }

    render () {
        return <ul>
            {this.states.todos.map((name) => {
                return <li>{name}</li>
            })}
        </ul>
    }
}
