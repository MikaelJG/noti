if (note_name = memorable) {
    touch output/note_name.cpp
}

( if : { else, elsif, else if} )

( iteration : { iterator, iteration, iterate } )

( breadth-first-search : { bfs, bf, breadth-first, breath }) 


{
    else: "path/to/if.cpp";
    elsif: "path/to/if.cpp";
    else if: "path/to/if.cpp";
}


keywords_array = [ else, elsif, else if, iterator, iteration, iterate ]


if ( requested_note_name in keywords_array) {

    note_name = note_name_hash.keyword 

    open examples/note_name.cpp
    
}
