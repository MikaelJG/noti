// see toggle for more

// all different options

toggle () {
    return this.state.timer ? this.state.pause() : this.state.play()
}

label () {
    return this.state.timer ? 'Pause' : 'Read'
}

render () {
    const method = this.state.timer ? this.pause : this.play
    const label = this.state.timer ? this.pause : this.play
    return <div>
    {this.state.timer ? 
        <button onClick={this.pause.bind(this)}>Pause</button>
        <button onClick={this.play.bind(this)}>Read</button>}
}
