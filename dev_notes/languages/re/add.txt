class TodoList extends React.Component {
    addItem (name) {
        const todos = [...this.state.todos, name]
        this.setState({todos: todos})
    }

    removeItem (name) {
        cons todos = this.todos.filter(t => t !== name)
        this.setState({todos: todos})
    }
}
