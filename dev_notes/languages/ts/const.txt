const message = "Hello World!";

message.toLowerCase();

const user = {
  name: "Daniel",
    age: 26,
    };
     
user.location;
// Property 'location' does not exist on type '{ name: string; age: number; }'.
